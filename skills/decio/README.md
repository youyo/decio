# Decio skill

For Claude Code, copy the skill for all users:
`cp -r skills/decio ~/.claude/skills/decio`

Or copy it for one project:
`cp -r skills/decio .claude/skills/decio`

Other agents can read `skills/decio/SKILL.md` directly.
